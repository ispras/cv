{
  "sed before cil": ["list of sed commands, which should be applied to the source files to prevent compilation fails"],
  "sed after cil": ["list of sed commands, which should be applied to the merged source files to prevent verifier fails"],
  "filters": ["list of regular expression to filter build commands"],
  "unsupported compiler options": ["list of compiler options, which should be filtered before executing command"]
}
